# {{PACKAGE_NAME}}

Platform-specific FileUni CLI binary for `{{TARGET}}`.

## Install

```bash
npm install {{PACKAGE_NAME}}
```

## Run

```bash
npx fileuni --help
```

This package is usually installed automatically as an optional dependency of `fileuni` on matching platforms.

## License

MIT
