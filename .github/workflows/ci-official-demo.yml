name: Official Demo CI

# This workflow is dispatched when OfficialDemo/ changes are detected
# by FileUni-WorkSpace/.github/workflows/trigger-public-project-ci.yml.

on:
  workflow_dispatch:
    inputs:
      workspace_ref:
        description: "Source ref in FileUni-WorkSpace (branch/tag/sha). Default: main"
        required: false
        default: "main"

permissions:
  contents: read

concurrency:
  group: ci-official-demo-${{ github.event.inputs.workspace_ref || github.run_id }}
  cancel-in-progress: true

env:
  WORKSPACE_REPO: FileUni/FileUni-WorkSpace

jobs:
  official-demo-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve workspace ref
        id: resolve-ref
        run: |
          ref="${{ github.event.inputs.workspace_ref }}"
          echo "ref=${ref:-main}" >> "$GITHUB_OUTPUT"

      - name: Checkout workspace
        uses: actions/checkout@v4
        with:
          repository: ${{ env.WORKSPACE_REPO }}
          ref: ${{ steps.resolve-ref.outputs.ref }}
          token: ${{ secrets.FILEUNI_PRIVATE_WORKSPACE_READ_TOKEN }}
          path: workspace
          submodules: true
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache Bun
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            workspace/OfficialDemo/node_modules
          key: official-demo-bun-${{ runner.os }}-${{ hashFiles('workspace/OfficialDemo/bun.lock') }}
          restore-keys: |
            official-demo-bun-${{ runner.os }}-

      - name: Verify OfficialDemo
        working-directory: workspace/OfficialDemo
        run: |
          bun install --frozen-lockfile
          bun run verify
